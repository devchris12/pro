Generate realistic mock data for: $ARGUMENTS

Rules:
- Match the exact shape of the existing API response schema
- Use realistic Nigerian retail figures (₦ currency, local product names where fitting)
- Create at least 8 data points spanning the last 30 days
- Output as a TypeScript const (not a Python file unless asked)
- Name the const mock + PascalCase of $ARGUMENTS
- Add a // MOCK DATA — replace with API call comment above the const
